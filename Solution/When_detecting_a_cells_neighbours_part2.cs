using System.Collections.Generic;
using System.Linq;
using FluentAssertions;
using Ploeh.AutoFixture;
using Ploeh.AutoFixture.Xunit;
using Xunit;

public class When_detecting_a_cell_s_neighbours_with_af
{
    /*
    [Fact]
    public void It_should_report_the_correct_number_for_a_cell_with_neighbours()
    {
        // Given
        var leftCell = new Cell(0, 1);
        var middleCell = new Cell(0, 2);
        var rightCell = new Cell(0, 3);
        var subject = new Generation(
                          new List<Cell>()
                          {
                              leftCell,
                              middleCell,
                              rightCell
                          });

        // When
        var neighbours = subject.NeighboursOf(0, 2);

        // Then
        neighbours.Should().Be(2);
    }
    */
}
